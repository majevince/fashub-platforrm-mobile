import { PlaceholderScreen } from '../../components/PlaceholderScreen';

export default function EventsScreen() {
  return (
    <PlaceholderScreen
      title="Events"
      note="Filters, RSVP, related events — ported from app/events in Step 4."
    />
  );
}
