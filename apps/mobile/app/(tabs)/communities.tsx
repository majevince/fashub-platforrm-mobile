import { PlaceholderScreen } from '../../components/PlaceholderScreen';

export default function CommunitiesScreen() {
  return (
    <PlaceholderScreen
      title="Communities"
      note="Discussion feed, member roles, trending tags — ported from app/communities in Step 4."
    />
  );
}
